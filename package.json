{
  "devDependencies": {
    "@types/emscripten": "^1.39.6",
    "axios": "^0.24.0",
    "html-webpack-plugin": "^5.5.0",
    "rimraf": "^3.0.2",
    "ts-loader": "^9.2.6",
    "typescript": "^4.5.2",
    "webpack": "^5.64.4",
    "webpack-cli": "^4.9.1",
    "webpack-dev-server": "^4.6.0",
    "@xmldom/xmldom": "^0.7.5",
    "xpath": "^0.0.32"
  },
  "name": "main",
  "version": "1.0.0",
  "main": "dist/main.js",
  "repository": "git@github.com:nyamadan/luna-base.git",
  "author": "Yoshiki YAMADA <nyamadandan@gmail.com>",
  "license": "MIT",
  "description": "My webpack project",
  "scripts": {
    "build": "rimraf -f build && mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release .. && cmake --build . && cd ../ && webpack --mode=production --node-env=production",
    "build:dev": "rimraf -f build && mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake --build . && cd ../ && webpack --mode=production --node-env=development",
    "build:prod": "rimraf -f build && mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release .. && cmake --build . && cd ../ && webpack --mode=production --node-env=production",
    "build:bin": "rimraf -f build && mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Release .. && cmake --build .",
    "watch": "webpack --watch",
    "serve": "webpack serve"
  },
  "private": true,
  "workspaces": [
    "./scripts/luna-base",
    "./scripts/main"
  ]
}
